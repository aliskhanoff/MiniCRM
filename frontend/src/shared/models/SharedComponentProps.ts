import type { ThemeColor, ThemeSize, ThemeType } from '@/shared'

export interface IElementColorTypeProps {
  color?: ThemeColor;
}

export interface IElementSizeProps {
  size?: ThemeSize;
}

export interface IElementThemeProps {
  theme?: ThemeType;
}